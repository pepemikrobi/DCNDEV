all:
  vars:
    pod_no: X
  hosts:
    "podX-srv11.sdn.lab":
      ansible_host: "pod{{ pod_no }}-srv11.sdn.lab"
      ansible_usern: "pod{{ pod_no }}"
    "podX-srv12.sdn.lab":
      ansible_host: "pod{{ pod_no }}-srv12.sdn.lab"
      ansible_user: "pod{{ pod_no }}"
